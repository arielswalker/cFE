name: Format Check

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  workflow_dispatch:
  schedule:
    # 11:30 PM UTC every Sunday
    - cron:  '30 23 * * 0'

jobs:
  format-check:
    name: Run format check
    uses: arielswalker/cFS/.github/workflows/format-check.yml@fix-946-make-format-check-reusable
